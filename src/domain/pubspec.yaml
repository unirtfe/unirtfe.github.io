name: arrugas_domain
description: Dominio de la applicación de arrugas

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  coolint: 1.0.0

flutter:
  generate: true
  uses-material-design: true
